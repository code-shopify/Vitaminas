<style>
  .shipping-info-section {
    background-color: #f9f3eb; /* Color de fondo similar al de la imagen */
    padding: 15px 20px;
    border-radius: 8px;
    max-width: 600px; /* Limita el ancho para que se parezca a la imagen */
    margin: auto;
  }

  .shipping-info-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .shipping-info-list li {
    display: flex;
    align-items: center;
    font-size: 14px;
    color: #635d57; /* Color de texto similar al de la imagen */
    margin-bottom: 8px;
  }

  .shipping-info-list img {
    width: 20px; /* Tamaño ajustado para los íconos */
    height: 20px;
    margin-right: 8px;
  }
</style>

<section class="shipping-info-section">
  <ul class="shipping-info-list">
    {% for block in section.blocks %}
      <li>
        {% if block.settings.image != blank %}
          <img src="{{ block.settings.image | image_url }}" alt="Ícono">
        {% endif %}
        <span>{{ block.settings.text }}</span>
      </li>
    {% endfor %}
  </ul>
</section>

{% schema %}
{
  "name": "Información de Envío",
  "settings": [],
  "blocks": [
    {
      "type": "feature",
      "name": "Característica",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Ícono de la característica"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Texto de la característica",
          "default": "Envío gratis en compras arriba de $1,299"
        }
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [
    {
      "name": "Información de Envío",
      "category": "Texto",
      "blocks": [
        {
          "type": "feature",
          "settings": {
            "text": "Envío gratis en compras arriba de $1,299"
          }
        },
        {
          "type": "feature",
          "settings": {
            "text": "Compra cifrada y segura"
          }
        },
        {
          "type": "feature",
          "settings": {
            "text": "Entrega garantizada de 3 a 6 días hábiles"
          }
        }
      ]
    }
  ]
}
{% endschema %}
