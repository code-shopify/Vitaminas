<div
  class="t4s-section-inner contenedor vitatu-section shadow mb-3 d-flex align-items-center justify-content-center"
  style="background-color: {{ section.settings.background_color }}; padding: 10px 0;"
>
  <div class="container">
    <div class="d-flex flex-wrap justify-content-center align-items-center gap-4">

      <!-- Iconos Personalizables -->
      {% for i in (1..6) %}
        {% assign image_key = 'icon_' | append: i | append: '_image' %}
        {% assign text_key = 'icon_' | append: i | append: '_text' %}
        {% assign image = section.settings[image_key] %}
        {% assign text = section.settings[text_key] %}

        {% if image != blank or text != blank %}
          <div class="d-flex align-items-center icon-block text-center">
            {% if image != blank %}
              <img src="{{ image | img_url: 'auto' }}" class="img-fluid icon-img me-2" alt="{{ text }}">
            {% endif %}
            <p class="fw-bold icon-text mb-0" style="color: {{ section.settings.text_color }};">
              {{ text }}
            </p>
          </div>
        {% endif %}
      {% endfor %}

    </div>
  </div>
</div>

{% style %}
  .contenedor {
    max-width: 100%;
    overflow: hidden;
    text-align: center;
  }

  /* Contenedor de cada icono y texto */
  .icon-block {
    display: flex;
    flex-direction: row; /* Asegura que el icono esté a la izquierda del texto */
    align-items: center;
    justify-content: center;
  }

  .icon-img {
    max-width: 20px; /* Tamaño más pequeño para alineación horizontal */
    height: auto;
  }

  .icon-text {
    font-size: 12px;
    text-align: left; /* Mantiene alineación horizontal */
    white-space: nowrap; /* Evita que el texto se divida en dos líneas */
  }

  @media (max-width: 767px) {
    .icon-img {
      max-width: 18px;
    }

    .icon-text {
      font-size: 10px;
    }
  }
{% endstyle %}

{% schema %}
{
  "name": "Sección de Iconos",
  "settings": [
    {
      "type": "color",
      "id": "background_color",
      "label": "Color de fondo",
      "default": "#DDE066"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Color del texto",
      "default": "#000000"
    },
    {
      "type": "image_picker",
      "id": "icon_1_image",
      "label": "Primera Imagen"
    },
    {
      "type": "text",
      "id": "icon_1_text",
      "label": "Texto 1",
      "default": "Sin Saborizantes Artificiales"
    },
    {
      "type": "image_picker",
      "id": "icon_2_image",
      "label": "Segunda Imagen"
    },
    {
      "type": "text",
      "id": "icon_2_text",
      "label": "Texto 2",
      "default": "Sin Lácteos"
    },
    {
      "type": "image_picker",
      "id": "icon_3_image",
      "label": "Tercera Imagen"
    },
    {
      "type": "text",
      "id": "icon_3_text",
      "label": "Texto 3",
      "default": "Sin Soya"
    },
    {
      "type": "image_picker",
      "id": "icon_4_image",
      "label": "Cuarta Imagen"
    },
    {
      "type": "text",
      "id": "icon_4_text",
      "label": "Texto 4",
      "default": "Sin Gluten"
    },
    {
      "type": "image_picker",
      "id": "icon_5_image",
      "label": "Quinta Imagen"
    },
    {
      "type": "text",
      "id": "icon_5_text",
      "label": "Texto 5",
      "default": "Sin Colorantes"
    },
    {
      "type": "image_picker",
      "id": "icon_6_image",
      "label": "Sexta Imagen"
    },
    {
      "type": "text",
      "id": "icon_6_text",
      "label": "Texto 6",
      "default": "Sin Endulzantes Artificiales"
    }
  ],
  "presets": [
    {
      "name": "Sección de Iconos",
      "category": "Custom"
    }
  ]
}
{% endschema %}

