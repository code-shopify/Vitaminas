{% schema %}
{
  "name": "Beneficios",
  "settings": [
    {
      "type": "color",
      "id": "background_color",
      "label": "Color de fondo",
      "default": "#FEF8F0"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Color del texto",
      "default": "#333333"
    },
    {
      "type": "text",
      "id": "icon_1",
      "label": "Ícono 1 (Emoji o HTML)",
      "default": "🚚"
    },
    {
      "type": "text",
      "id": "text_1",
      "label": "Texto 1",
      "default": "Envío gratis en compras arriba de $1,299"
    },
    {
      "type": "text",
      "id": "icon_2",
      "label": "Ícono 2 (Emoji o HTML)",
      "default": "🔒"
    },
    {
      "type": "text",
      "id": "text_2",
      "label": "Texto 2",
      "default": "Compra cifrada y segura"
    },
    {
      "type": "text",
      "id": "icon_3",
      "label": "Ícono 3 (Emoji o HTML)",
      "default": "📦"
    },
    {
      "type": "text",
      "id": "text_3",
      "label": "Texto 3",
      "default": "Entrega garantizada de 3 a 6 días hábiles"
    }
  ],
  "presets": [
    {
      "name": "Beneficios",
      "category": "Información"
    }
  ]
}
{% endschema %}

<div class="benefits-section" style="background-color: {{ section.settings.background_color }};">
  <div class="benefits-container">
    <div class="benefit-item">
      <span class="benefit-icon">{{ section.settings.icon_1 }}</span>
      <p class="benefit-text" style="color: {{ section.settings.text_color }};">{{ section.settings.text_1 }}</p>
    </div>

    <div class="benefit-item">
      <span class="benefit-icon">{{ section.settings.icon_2 }}</span>
      <p class="benefit-text" style="color: {{ section.settings.text_color }};">{{ section.settings.text_2 }}</p>
    </div>

    <div class="benefit-item">
      <span class="benefit-icon">{{ section.settings.icon_3 }}</span>
      <p class="benefit-text" style="color: {{ section.settings.text_color }};">{{ section.settings.text_3 }}</p>
    </div>
  </div>
</div>

<style>
.benefits-section {
  padding: 15px;
  border-radius: 10px;
}

.benefits-container {
  display: flex;
  justify-content: space-around;
  align-items: center;
  max-width: 900px;
  margin: auto;
  flex-wrap: wrap;
}

.benefit-item {
  display: flex;
  align-items: center;
  gap: 10px;
  margin: 5px 0;
}

.benefit-icon {
  font-size: 20px;
}
</style>
